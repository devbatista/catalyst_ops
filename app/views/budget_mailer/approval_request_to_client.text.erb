Olá, <%= @client.name %>.

O <%= @sender_name %> da empresa <%= @company_name %> está lhe enviando o orçamento #<%= @budget.code %> para sua aprovação.

Para aprovar ou rejeitar, acesse:
<%= @approval_url %>

Validade do orçamento: <%= l(@budget_valid_until, format: "%d/%m/%Y") %>.

Este link expira em <%= l(@approval_expires_at.to_date, format: "%d/%m/%Y") %>.
