<% content_for :title, "Assinatura CatalystOps - Boleto gerado" %>

<h2 style="margin-top:0;">Sua assinatura está prestes a ser renovada!</h2>

<p style="font-size:16px; line-height:1.6;">
  Olá, <strong><%= (@company.respond_to?(:responsible) && @company.responsible&.name.presence) || @company.name %></strong>.
</p>

<p style="font-size:16px; line-height:1.6;">
  Sua assinatura CatalystOps foi ciclada e está aguardando pagamento via boleto.
</p>

<p style="font-size:14px; line-height:1.6; color:#555;">
  Assim que o pagamento for confirmado, sua conta será renovada automaticamente.
</p>

<% if @expires_on.present? %>
  <p style="font-size:14px; color:#666;">
    Vencimento do boleto: <%= I18n.l(@expires_on.to_date, format: :long, locale: :'pt-BR') rescue @expires_on %>
  </p>
<% end %>

<% if @status.present? %>
  <p style="font-size:14px; color:#666;">
    Status do pagamento: <strong><%= @status.to_s.capitalize %></strong>.
  </p>
<% end %>

<% if @url.present? %>
  <div style="margin:24px 0;">
    <a href="<%= @url %>" target="_blank" rel="noopener"
       style="background:#0d6efd; color:#fff; text-decoration:none; padding:14px 32px; border-radius:4px; font-size:16px; font-weight:bold; display:inline-block;">
      Baixar boleto
    </a>
  </div>
<% end %>

<% if @barcode.present? %>
  <p style="font-size:14px; line-height:1.6;">
    Linha digitável do boleto:
  </p>
  <p style="font-family:monospace; font-size:14px; background:#f7f7f7; padding:10px 12px; border-radius:4px;">
    <%= @barcode %>
  </p>
<% end %>

<p style="font-size:14px; color:#888;">
  Em caso de dúvidas, responda este e-mail ou fale com nosso suporte.
</p>